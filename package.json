{
  "name": "seek-job-notification-2.0",
  "private": true,
  "description": "Run from repo root: npm run dev (API) and npm run client (UI) in two terminals.",
  "scripts": {
    "dev": "npm run dev --prefix server",
    "dev:server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev:client": "npm run dev --prefix client"
  }
}
